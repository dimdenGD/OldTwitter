name: Pack Extension

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ] 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Tree
        uses: actions/checkout@v3

      - name: Setup Node.js environment
        uses: actions/setup-node@v2.5.2
        
      - name: Setup NPM packages
        run: npm install
        
      - name: Pack extension
        run: npm run build

      - name: Unzip extension
        uses: montudor/action-zip@v1.0.0
        with:
          args: unzip -qq D:\a\OldTwitter\OldTwitterChrome.zip -d D:\a\OldTwitter\OldTwitterChrome; unzip -qq D:\a\OldTwitter\OldTwitterFirefox.zip -d D:\a\OldTwitter\OldTwitterFirefox
        
      - name: Upload for Firefox
        uses: actions/upload-artifact@v3.1.2
        with:
          name: OldTwitterFirefox.zip
          path: D:\a\OldTwitter\OldTwitterFirefox
      - name: Upload for Chromium
        uses: actions/upload-artifact@v3.1.2
        with:
          name: OldTwitterChrome.zip
          path: D:\a\OldTwitter\OldTwitterChrome
